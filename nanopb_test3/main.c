#include "pb_decode.h"
#include "pb_encode.h"
#include "test.pb.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    MyMessage msg = MyMessage_init_zero;

    // Allocate memory for sub_messages
    sizeof(msg.sub_messages);
    sizeof(SubMessage);
}
